/**
 * User status constants for the Vallle app.
 * Matches the `status` column on the `users` table.
 *
 * - ACTIVE: User can sign in and use the app.
 * - INACTIVE: User is deactivated — sign-in blocked.
 */
export const USER_STATUSES = {
  ACTIVE: "active",
  INACTIVE: "inactive",
};
