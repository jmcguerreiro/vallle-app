/**
 * Component: Loader
 * Displays a three-dot loading animation.
 * @component
 * @returns {JSX.Element}
 */
const Loader = () => {
  // Render
  return <div className="c-loader" />;
};

export default Loader;
